# Used by docker compose for free Let's Encrypt SSL (Caddy).
# Point this domain's A record at your server, then: docker compose up -d

SITE_DOMAIN=your-domain.in
LETSENCRYPT_EMAIL=your.email@gmail.com
